#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define endl "\n"
#define rep(i,a,b) for(int i = a; i<b; i++)

    int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int i,j,k;
    
    int r, c;
    scanf("%ld %ld", &r, &c); 

    vector<char*[]> grid; 

    rep(i, 0, r){
        char* buffer[c]; 
        grid.push_back(buffer) 
    }


    
    return 0;
}